# ADR-0004 — Progressive context

**Status:** FROZEN

Agent entrypoints act as maps. Context depth is selected by task mode and loaded progressively rather than reconstructing full project history at startup.
