# ADR-0005 — Capability ownership

**Status:** FROZEN

Noema owns `CapabilityRef` semantics. Skill Foundry owns concrete reusable capability implementations, taxonomy, ontology, and Skill-specific evaluation.
