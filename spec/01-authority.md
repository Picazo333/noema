# Authority Model

Authority is federated. Each material domain declares what it owns and what it explicitly does not own. Noema validates boundaries; it does not centralize them.

A material authority boundary should have one authorized writer class. External agents and capabilities inherit only the write scope explicitly granted by the current project/task.

`consults`, `consumes`, `publishes-to`, and `harvests-to` relationships do not transfer authority.
