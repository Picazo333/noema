# Capabilities and Stack

Noema owns the interoperability contract around capability references and executor descriptors. It does not own Skill Foundry's taxonomy, ontology, or capability implementations.

Executor descriptors are relatively stable and versionable. Runtime availability (quota, resets, outages, session constraints) is volatile and lives outside Git by default.

Capabilities are selected before executors. Provider identity is not architecture.
