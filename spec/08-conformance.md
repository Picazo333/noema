# Conformance

`noema lint` is fast, deterministic, and CI-oriented. It validates machine-verifiable declarations and references.

`noema audit` is diagnostic and deeper. It may produce `UNASSESSED` findings for quality claims or concerns that require runtime, human, or evidence review.

Result severities are `ERROR`, `WARNING`, and `INFO`; the protocol intentionally defines no global Noema score.
