# Extension Model

Domain-specific metadata belongs under namespaced extension keys beginning with `x-`. Noema Core preserves unknown extensions without interpreting them.

Repeated cross-project extension needs may later justify a formal shared extension contract; promotion is evidence-driven rather than speculative.
